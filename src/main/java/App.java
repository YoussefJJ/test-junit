import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class App {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*System.setProperty("webdriver.chrome.driver", "C:\\Users\\Youssef\\Documents\\TP Selenium\\chromedriver.exe");
        WebDriver driver = new ChromeDriver();
        driver.get("http://todomvc.com");
        driver.findElement(By.partialLinkText("Backbone.js")).click();
        driver.findElement(By.className("new-todo")).sendKeys("Meet a Friend");
        driver.findElement(By.className("new-todo")).sendKeys(Keys.ENTER);
        driver.findElement(By.className("new-todo")).sendKeys("Buy Meat");
        driver.findElement(By.className("new-todo")).sendKeys(Keys.ENTER);
        driver.findElement(By.className("new-todo")).sendKeys("Clean the Car");
        driver.findElement(By.className("new-todo")).sendKeys(Keys.ENTER);
        driver.findElement(By.xpath("/html/body/section/section/ul/li[1]/div/input")).click();*/
	}

}
